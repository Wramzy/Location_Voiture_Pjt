{% extends 'base.html.twig' %}

{% block title %}Modifier location{% endblock %}

{% block body %}
<div class="page-header">
    <div class="d-flex justify-content-between align-items-start flex-wrap gap-3">
        <div>
            <h1 class="page-title">Modifier la location</h1>
            <p class="page-subtitle">Mettez à jour les informations de la location</p>
        </div>
        {{ include('rental/_delete_form.html.twig') }}
    </div>
</div>

{{ include('rental/_form.html.twig', {
    button_label: 'Mettre à jour',
    cancel_path: path('app_rental_index')
}) }}
{% endblock %}
